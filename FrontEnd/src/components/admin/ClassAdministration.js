import React from "react";
import {connect} from "react-redux";

function classAdministration() {
    return (
        <div>
            Klassenverwaltung
        </div>
    )
}

export default connect()(classAdministration)
